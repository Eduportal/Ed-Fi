@model MembershipUserCollection

@{
    ViewBag.Title = "Users";
}

<h2>Users</h2>

<table>
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.ToString()</td>
            </tr>
        }
    </tbody>
</table>
